language: python

python:
  - '2.7'
  - '3.3'

env:
  - DJANGO=""
#  - DJANGO="https://github.com/django/django/archive/1.7b1.zip"

install:
  - 'pip install -e .[dev]'
#  - 'if ( test -n "$DJANGO" ); then pip install $DJANGO; fi'

script:
  - 'cd ./src'
  - 'python manage.py test --settings richard.settings.testing'

notifications:
  irc:
    channels:
      - "irc.freenode.org#richard"
    on_success: change
    on_failure: always
